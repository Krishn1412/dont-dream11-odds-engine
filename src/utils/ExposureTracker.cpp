// src/utils/EventProcessor.cpp (or another .cpp file, but not multiple .cpp files)
#include "ExposureTracker.h"

// Define the global and batch exposure
ExposureState globalExposure;
ExposureState batchExposure;
